# Audit: commit 2462a62

Scope checked: the 16 `src/` files listed by `git show 2462a62 --name-only -- src/`; references searched in `src/`, `scripts/`, `extensions/`, `test/`, and `bin/`.

## src/adapters/pi.ts

| symbol | what changed | stale site file:line | suggested one-line fix |
|---|---|---|---|
| — | No stale references found. | — | — |

## src/agent/harness-bridge.ts

| symbol | what changed | stale site file:line | suggested one-line fix |
|---|---|---|---|
| — | No stale references found; both new exports are imported by `extensions/pi/index.ts` and `extensions/omp/index.ts`. | — | — |

## src/backends/backend.ts

| symbol | what changed | stale site file:line | suggested one-line fix |
|---|---|---|---|
| `foregroundProcesses` → `paneForeground` | Backend interface member was renamed and its return shape changed to `PaneForeground`. | — | No stale current-tree references found. |

## src/backends/herdr/cli.ts

| symbol | what changed | stale site file:line | suggested one-line fix |
|---|---|---|---|
| `herdrJSON` internals / new `herdrAck` | JSON command execution was split so acknowledgement-only commands do not parse JSON. | — | No stale current-tree references found; `herdrAck` is imported by `src/backends/herdr/index.ts`. |

## src/backends/herdr/index.ts

| symbol | what changed | stale site file:line | suggested one-line fix |
|---|---|---|---|
| `foregroundProcesses` → `paneForeground` | Backend implementation now returns `PaneForeground`. | — | No stale current-tree references found. |
| `splitAgainst` → `reseatIntoGroup` | Private helper was renamed and its target argument was removed. | — | No stale current-tree references found. |

## src/backends/herdr/notify.ts

| symbol | what changed | stale site file:line | suggested one-line fix |
|---|---|---|---|
| `available` gating | Availability changed from `HERDR_ENV`/PATH to configured backend plus reachability. | — | No stale current-tree references found. |

## src/backends/pane-ready.ts

| symbol | what changed | stale site file:line | suggested one-line fix |
|---|---|---|---|
| `PaneForeground`, `paneAtShellPrompt`, `paneRunsCommand`, `sleepMs` | New shared exports moved pane state and synchronous sleep helpers out of backend/lifecycle files. | — | No stale current-tree references found; all production consumers import the new helpers. |

## src/backends/tmux/index.ts

| symbol | what changed | stale site file:line | suggested one-line fix |
|---|---|---|---|
| local `sleepMs` → shared `sleepMs` | Removed duplicate helper and imported `src/backends/pane-ready.ts`. | — | No stale current-tree references found. |

## src/bridge-bundle.ts

| symbol | what changed | stale site file:line | suggested one-line fix |
|---|---|---|---|
| `orchestrator-bridge` → `pi-bridge` | Shipped extension name changed; old name retained only in `RETIRED_EXTENSION_NAMES` for cleanup. | — | No stale current-tree references found beyond the intentional retired-name constant. |

## src/commands/daemon.ts

| symbol | what changed | stale site file:line | suggested one-line fix |
|---|---|---|---|
| `callerWorkspace()` → `actorWorkspace(actor)` | Daemon enrichment now derives workspace from the acting token when needed. | — | No stale current-tree references found. |

## src/commands/lifecycle.ts

| symbol | what changed | stale site file:line | suggested one-line fix |
|---|---|---|---|
| `foregroundProcesses`/`string[]` → `paneForeground`/`PaneForeground` | Pane checks use the new structured foreground state. | — | No stale current-tree references found. |
| local `sleepMs` → shared `sleepMs` | Removed duplicate helper. | — | No stale current-tree references found. |

## src/commands/spawn.ts

| symbol | what changed | stale site file:line | suggested one-line fix |
|---|---|---|---|
| `tileAgentIntoGroup` (new export) | New function replaced direct placement calculation and is exported, but has no import anywhere in the searched tree; its only use is internal to this file. | `src/commands/spawn.ts:542` | Remove `export` (or add an actual external import if public API use is intended). |
| `placeAgent` | Signature dropped the `placement` parameter. | — | All current callers pass the new five-argument shape. |
| `launchAdditionalAgents` → `growFleetIntoGroup` | Helper renamed and now returns its created-agent array. | — | No stale current-tree references found. |
| `resolveSpawnWorkspace` | Signature changed from one `requested` argument to `(settings, backend)`. | — | Current caller at `src/commands/spawn.ts:676` passes both arguments. |

## src/commands/target.ts

| symbol | what changed | stale site file:line | suggested one-line fix |
|---|---|---|---|
| `callerWorkspace` use in ownership path → `actorWorkspace(token)` | Ownership workspace resolution changed to use the acting token fallback. | — | No stale current-tree references found; remaining `callerWorkspace()` calls are intentional caller-placement queries. |

## src/daemon/work-loop.ts

| symbol | what changed | stale site file:line | suggested one-line fix |
|---|---|---|---|
| local `sleepMs` → shared `sleepMs` | Removed duplicate helper. | — | No stale current-tree references found. |

## src/doctor/extensions.ts

| symbol | what changed | stale site file:line | suggested one-line fix |
|---|---|---|---|
| new `presenceRootFault` check | Doctor now imports and checks the new presence-root validator. | — | No stale current-tree references found. |

## src/presence/store.ts

| symbol | what changed | stale site file:line | suggested one-line fix |
|---|---|---|---|
| new `presenceRootFault` | Added validator is imported by `src/doctor/extensions.ts`. | — | No stale current-tree references found. |

## CLEAN

No stale references found in these touched files: `src/adapters/pi.ts`, `src/agent/harness-bridge.ts`, `src/backends/backend.ts`, `src/backends/herdr/cli.ts`, `src/backends/herdr/index.ts`, `src/backends/herdr/notify.ts`, `src/backends/pane-ready.ts`, `src/backends/tmux/index.ts`, `src/bridge-bundle.ts`, `src/commands/daemon.ts`, `src/commands/lifecycle.ts`, `src/commands/target.ts`, `src/daemon/work-loop.ts`, `src/doctor/extensions.ts`, `src/presence/store.ts`.

`src/commands/spawn.ts` has no stale old-name or old-argument call sites; its sole finding is the unimported new `tileAgentIntoGroup` export documented above.
