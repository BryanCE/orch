# 22:54 suite failure triage

Ground truth: `test-results.md` (822 pass, 27 fail). No gates run here. No failures are FIXED-SINCE-RUN; the current failures are either an in-flight owned slice or still reproducible from the current tree.

| test | file | class | cause | smallest fix | dispatch-to |
|---|---|---|---|---|---|
| review plumbing > lists only done worktree agents with commits ahead | `test/review.test.ts` | NEEDS-FIX | Child CLI opens the already-stamped store, then `openStore()` re-runs non-idempotent `REBUILD_DDL`; `CREATE TABLE harnesses` throws. `spawnedRecords()` catches the error and review sees zero rows. | In `src/store/connection.ts`, initialize DDL only for a new/recreated store (or make the complete initialization idempotent); do not execute `CREATE TABLE`/index/view/trigger DDL on every reopen. | unassigned: store-connection |
| review plumbing > reject re-dispatches feedback through the adapter inbox | `test/review.test.ts` | NEEDS-FIX | Same store-reopen `table harnesses already exists` failure makes `findReviewItem()` empty. | Same `openStore()` initialization fix. | unassigned: store-connection |
| review plumbing > approve merges and removes the worktree and branch | `test/review.test.ts` | NEEDS-FIX | Same store-reopen failure hides the registered review row before merge. | Same `openStore()` initialization fix. | unassigned: store-connection |
| run rows > stays readable after the agent presence directory is deleted | `test/store-runs.test.ts` | NEEDS-FIX | Reopening the current-schema DB executes non-idempotent rebuild DDL and fails at `harnesses`; this is not caused by the `STORE_SCHEMA = 6` stamp or `PRAGMA foreign_keys = ON`. | Make `openStore()` skip already-created current-schema DDL (while retaining fresh-store creation and schema validation). | unassigned: store-connection |
| herdr and notification hardening > uses a non-empty agent name and preserves shell command as one argv value | `test/herdr-notify-hardening.test.ts` | NEEDS-FIX | Test still asserts the retired `pane run bash -lc ...` launch, while current `HerdrBackend.spawn()` intentionally uses `agent start --kind ... --pane ...` (as the updated backend-herdr tests require). | Update this test’s launch assertion to the canonical `agent start` argv; keep the non-empty rename assertion. | unassigned: herdr test update |
| herdr and notification hardening > falls back to a real name when an adapter id is blank | `test/herdr-notify-hardening.test.ts` | NEEDS-FIX | `paneName()` falls back to `agent`, but `spawn()` then indexes `HERDR_KINDS[""]` and throws instead of selecting a valid harness kind. | Decide/implement a closed fallback kind (smallest for this hardening seam: use the test’s intended pi kind when id is blank), or make the fixture adapter id valid; update `src/backends/herdr/index.ts`/test together. | unassigned: herdr spawn |
| queue replay keeps typed scope > a reopened store offers pack work only to that pack | `test/queue-workspace-replay.test.ts` | OWNED | Reopen hits the same rebuild-DDL failure, but queue replay is explicitly in the queue-rewire slice. | Queue-rewire should make queue/task reopen use the one current store initializer; preserve typed scope filtering. | queue-rewire |
| event store rows > appendEvent keeps sequence numbers across store reopen | `test/store-events.test.ts` | NEEDS-FIX | Current-schema reopen fails on `CREATE TABLE harnesses`; the schema-6 bump correctly rejects old stores but does not make a current store reopenable. | Fix `src/store/connection.ts` initialization-once/idempotence; no event-sequence change needed. | unassigned: store-connection |
| fleet ownership scoping > close --all works without an owner token | `test/owner-scoping.test.ts` | NEEDS-FIX | Child `close --all` cannot read registry because reopening the DB throws; the record remains. | Same `openStore()` reopen fix. | unassigned: store-connection |
| fleet ownership scoping > explicit foreign target closes successfully | `test/owner-scoping.test.ts` | NEEDS-FIX | Target resolution fails on the same `harnesses already exists` reopen error. | Same `openStore()` reopen fix. | unassigned: store-connection |
| fleet ownership scoping > driving verbs remain gated against a live foreign holder | `test/owner-scoping.test.ts` | NEEDS-FIX | Governance never reaches the owner check; target lookup dies in store initialization, so the error omits `other-orchestrator`. | Same `openStore()` reopen fix, then retain the existing owner gate. | unassigned: store-connection |
| fleet ownership scoping > result refuses a foreign-owned agent and names its owner | `test/owner-scoping.test.ts` | NEEDS-FIX | `selectSpawnedRecord()` fails during current DB reopen before ownership can be checked. | Same `openStore()` reopen fix. | unassigned: store-connection |
| fleet ownership scoping > pane mutations refuse a foreign-owned agent and name its owner | `test/owner-scoping.test.ts` | NEEDS-FIX | Same target-resolution/store-reopen exception masks the expected foreign-owner refusal. | Same `openStore()` reopen fix. | unassigned: store-connection |
| fleet ownership scoping > close ignores --force and remains unconditional | `test/owner-scoping.test.ts` | NEEDS-FIX | The functional close subprocess cannot reopen the registry, so it exits before exercising unconditional close. (Separate design issue: recon notes that accepting retired `--force` should eventually be removed.) | Fix store reopen first; then keep close ungated and separately retire `--force` if required by the current spec. | unassigned: store-connection; follow-up lifecycle |
| a spawned agent touches only what it spawned > close --all sweeps every managed spawn | `test/owner-scoping.test.ts` | NEEDS-FIX | Child bulk close cannot enumerate the existing registry after the non-idempotent DDL rerun. | Same `openStore()` reopen fix. | unassigned: store-connection |
| a spawned agent touches only what it spawned > close from a spawned agent is unconditional | `test/owner-scoping.test.ts` | NEEDS-FIX | Explicit target lookup fails during store reopen, before unconditional close. | Same `openStore()` reopen fix. | unassigned: store-connection |
| a spawned agent touches only what it spawned > the workspace operator keeps control of an agent-owned fleet | `test/owner-scoping.test.ts` | NEEDS-FIX | Same current-schema DB initialization exception masks the intended workspace-operator close path. | Same `openStore()` reopen fix. | unassigned: store-connection |
| loadConfig > parses every supported settings section | `test/config.test.ts` | NEEDS-FIX | `src/config.ts` now intentionally normalizes the decided `fleet.pack_cap` default (10), but this expected object omits it. | Add `pack_cap: 10` to the expected normalized fleet object (or explicitly remove the new setting, contrary to A12). | unassigned: config tests |
| loadConfig > applies every settings default when sections are absent | `test/config.test.ts` | NEEDS-FIX | Same intentional `pack_cap: 10` normalization drift; implementation matches the decided pack-cap default. | Add `pack_cap: 10` to the expected default object. | unassigned: config tests |
| writeSettingsFullTree > round-trips defaults without inventing max_agents | `test/config.test.ts` | NEEDS-FIX | Full-tree writer now persists the new default `fleet.pack_cap`; assertion still expects the pre-pack-cap shape. | Include `pack_cap: 10` in the expected `raw.fleet` object; retain the no-`max_agents` assertion. | unassigned: config tests |
| presence status schema > persists the complete spawned identity record | `test/presence-schema.test.ts` | OWNED | The record write/reopen path is part of spawn/presence registration, and currently also trips the non-idempotent rebuild DDL. | Spawn-registry should finish the normalized spawn/presence writer and use the corrected current-store open path. | spawn-registry |
| pi-bridge command-lock interception > wraps a matching locked command in acquire→release around the tool call | `test/cmd-lock-bridge.test.ts` | NEEDS-FIX | The matching interception path does not leave a lock visible after `tool_execution_start`; likely seam is swallowing the locked-command/config path (the handler in `src/agent/tools.ts` should acquire before return). `lockedCommandPatterns()` also silently converts any config-load failure to `[]`, hiding diagnosis. | Make the bridge’s bash start handler reliably load the configured patterns and await/store `acquireCommandLock()` for the tool-call id; do not swallow a valid settings load. Verify release remains keyed by that id. | unassigned: pi bridge |
| spawn limits > schema loads global and workspace caps | `test/spawn-limits.test.ts` | NEEDS-FIX | Same intentional new `fleet.pack_cap: 10` field appears in normalized config but the assertion is old. | Add `pack_cap: 10` to the expected fleet object. | unassigned: config tests |
| spawn limits > omitted fleet caps normalize to defaults | `test/spawn-limits.test.ts` | NEEDS-FIX | New normalization includes `pack_cap: 10` and preserves `max_agents: undefined`; old expected object omits both. | Update the expected shape to include `pack_cap: 10` and `max_agents: undefined` (or align the test fixture/schema contract). | unassigned: config tests |
| retention sweep > uses each table's own window and keeps queued and claimed tasks | `test/retention.test.ts` | NEEDS-FIX | Test seed calls `recordTaskDone()` (attempt `since` is now) and then writes an old `until`; strict `CHECK (until IS NULL OR until > since)` rejects the fixture before sweep. Once fixed, retain the known separate bug: `src/daemon/retention.ts` adds deleted agent rows and deleted presence dirs, double-counting one ended agent. | Seed a historical attempt with `since < until` (or pass historical instants through `claimTask`/settlement) in `test/retention.test.ts`; also dedupe `ended_agents` count in `src/daemon/retention.ts` so one logical agent is counted once while both artifacts are removed. | unassigned: retention |
| retention sweep > returns zero counts when every row is inside its window | `test/retention.test.ts` | NEEDS-FIX | Same invalid historical attempt fixture (`until ==/before since`) aborts before the zero-count assertion. | Fix `seedQueueTask()` timestamps as above; keep the retention count dedupe fix for the known ended-agent case. | unassigned: retention |
| close always works > dead pane-less close is a successful no-op that reaps registry and presence | `test/close-always.test.ts` | NEEDS-FIX | Child close target resolution cannot reopen the current DB because `REBUILD_DDL` contains unconditional `CREATE TABLE harnesses`; it exits before reaping. `lint-misc` owns lint for this file, not this functional failure. | Fix `src/store/connection.ts` reopen initialization; then verify dead/no-pane close reaps both registry and presence. | unassigned: store-connection |

## Cross-cutting notes

- The schema-6 bump is correct for the pre-publish single-shape rule. The reopen failure is caused by unconditional rebuild DDL, not by foreign-key enforcement; `PRAGMA foreign_keys = ON` should remain.
- The dominant blocker is one `src/store/connection.ts` fix: current-schema stores must be opened without replaying non-idempotent rebuild `CREATE TABLE`/index/view/trigger statements. It explains review, run-row, event, ownership, spawned-agent, and close failures.
- `src/daemon/retention.ts` has the already-known ended-agent double-count (`recordsRemoved + result.removed.length`); fix count semantics independently of the fixture CHECK failure.
