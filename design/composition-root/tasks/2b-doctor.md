# 2b-doctor

Model: luna:low
Owns: `src/commands/doctor.ts`. Follow `2b-README.md`.

Sites:
- `:29`, `:39`, `:47` `runDoctor(orchDir())` → `runDoctor(services.orchDir)`

Tests: `grep -l "commands/doctor" test/*.ts`.
