# 2b-models

Model: luna:low
Owns: `src/commands/models.ts`. Follow `2b-README.md`.

Sites:
- `:56` `commandLogger().error("models.catalogue-failed", ...)`
- `:155` `settings = loadSettings(orchDir());`

Tests: `grep -l "commands/models" test/*.ts`.
