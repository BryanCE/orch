## 0. Sequencing gate (do not skip)

- [ ] 0.1 Confirm all five behavioral siblings are archived: `openspec list` shows `adapter-control-authority`, `adapter-presence-writers`, `settings-json-config`, `provider-driven-setup-doctor`, and `tmux-backend-completion` no longer under `openspec/changes/`. Do NOT start any phase below until this holds.
- [ ] 0.2 Capture the baseline: run `bun run check` and `bun test` green on the current tree, and record the current doctor check ids (`orch doctor`) so later phases can prove the set is unchanged.

## 1. Phase A — `src/commands.ts` → `src/commands/`

- [x] 1.1 Create `src/commands/target.ts`: move `resolveTarget`, `backendTarget`, `callerWorkspace`, `requirePresenceTarget`, `livePanePresenceEntries`, `targetHost`, `remoteCommandArgs`, `remoteWrite`, and the shared helpers (`die`, `splitOptionFlags`, `parseTargetPrompt`, `firstNonEmptyText`, `resultText`). This is the shared base other command modules import. (2026-07-17: verified on disk — `src/commands/target.ts` exists)
- [x] 1.2 Create `src/commands/status.ts` (view derivation + tables), `spawn.ts`, `control.ts`, `lifecycle.ts`, `panes.ts`, `results.ts`, `events.ts`, `review.ts`, `queue.ts`, `clean.ts`, `daemon.ts`, `setup.ts` per the design D1 mapping — moving each function verbatim (no logic edits) into its domain module, importing narrowed inputs from `target.ts`. (2026-07-17: all 13 domain modules present under `src/commands/`)
- [x] 1.3 Create `src/commands/index.ts` holding ONLY the argv→command dispatch table, `usage`, version read, and first-run detection. It imports command fns from the domain modules and contains no presence I/O, no `adapter.`/`backend.` calls, no `loadConfig`/`resolveTarget`/`resolveBackend`. (2026-07-17: grep confirms zero loadConfig/resolveTarget/resolveBackend in index.ts)
- [x] 1.4 Repoint every importer of `src/commands.ts` (`bin/orch.ts`, `src/daemon/*`, any test) to the new module paths; delete `src/commands.ts`. (2026-07-17: `src/commands.ts` deleted; repo-wide grep finds zero remaining importers)
- [ ] 1.5 Gate: `bun run check` exits 0, `bun run check:bridge` passes (the port-boundary check must stay green — command wrappers must not reintroduce a concrete-adapter import or wire literal), and `bun test` passes (WSL: give CLI-spawning/git-heavy tests 15–30s timeouts). Also run `wc -l src/commands/*.ts` and assert every module is ≤ 700 lines (the tree-wide sweep in task 7.1 still runs). Fix repoints until green.

## 2. Phase B — harness layout + `extensions/pi/index.ts` split

- [x] 2.0 Harness directory move (CLAUDE.md Rule 10, design D6): `git mv` `extensions/orchestrator-bridge.ts`→`extensions/pi/index.ts`, `scripts/claude-hooks.ts`→`extensions/claude/index.ts`, `scripts/codex-notify.ts`→`extensions/codex/index.ts`, `src/presence-schema.ts`→`src/presence/schema.ts`; rewrite the moved files' `../src/`→`../../src/` imports; repoint `package.json` build:ext/build:hooks/build:notify sources; repoint the four tests that referenced old paths by string. All bundle OUTPUT names preserved. (2026-07-19)
- [x] 2.0a Fix `scripts/check-bridge.ts`: its `extensions` scan was non-recursive, so once `extensions/` held only subdirectories it scanned ZERO files and still printed OK. Made recursive. (2026-07-19 — latent silent-pass bug the move exposed)
- [x] 2.2 Update `src/bridge-bundle.ts` so the source directory resolves via a `PI_EXTENSION_SOURCE_DIR` name→dir map (`"orchestrator-bridge" → "pi"`) rather than being derived from the name; keep `PI_EXTENSION_NAMES = ["orchestrator-bridge"]` and the output `dist/extensions/orchestrator-bridge.js` unchanged. (2026-07-19: implemented as a map, deliberately decoupling artifact name from source dir — see design D2)
- [x] 2.1 Split `extensions/pi/index.ts` (1,868 lines) into FOUR modules — `presence.ts` (pi's binding to the shared writer: `computeKey`, `extractText`, `truncate`, pi-side status/result/inbox calls), `daemon-ack.ts` (orchd socket ack transport, `ackedMessageIds` dedupe), `tools.ts` (`orch_*` tool/command registration + pi-event guards), `index.ts` (composition + default export). Move verbatim.
- [x] 2.1a Move ALL herdr-gated code out of the harness directory into `src/backends/herdr/hud.ts`: `sendHerdrMetadata`, `nextMetadataSeq`, `bridgeWorkspace*`, `bridgeNotificationText`, `notifyHerdr`, `registerHerdrPaneState`, the `herdr:blocked` handlers, socket RPC over `HERDR_SOCKET_PATH`, and the `herdr notification`/`pane list` shell-outs. It is backend-gated, not agent-gated — filing it under a harness dir is the harness×plexer pair code Rule 9 forbids. Verify `grep -rln 'HERDR_SOCKET_PATH\|herdr:blocked\|registerHerdrPaneState' extensions/` returns nothing.
- [ ] 2.3 Rebuild the bundle: `bun run build:dev`. Run `orch doctor` and confirm the bridge extension-staleness check passes (the hash changes once, then stabilizes).
- [ ] 2.4 Gate: `bun run check` exits 0, `bun run check:bridge` passes (confirm its reported file count is non-zero and includes the `extensions/*/` files — a zero count means the recursion regressed), and `bun test` passes.

## 3. Phase C — `src/doctor.ts` → `src/doctor/`

- [x] 3.1 Create `src/doctor/` modules: `shared.ts` (leaf utils `readJson`/`pidAlive`/`hasErrorCode`/`commandOutput`/`onPath`), one module per check group (`bins`, `presence`, `backends`, `extensions`, `daemon`, `notify`, `remote`, `config`), and `runner.ts` (`runDoctor`, `applyFixes`, `isolated`, shared `CheckResult` type). Move verbatim. (2026-07-19: `shared.ts` + `bins/presence/backends/extensions/daemon/notify/remote/config` + `runner.ts`; verbatim — a line-multiset diff against the old monolith reports zero missing/changed lines. No `hooks.ts`: `checkClaudeHooks` no longer exists in doctor, it moved to the claude adapter's `diagnoseShim` in an earlier sibling.)
- [x] 3.2 Repoint every importer of `src/doctor.ts` (`commands/setup.ts`, daemon, tests); delete `src/doctor.ts`. (2026-07-19: `src/doctor.ts` deleted, no stub.)
- [ ] 3.3 Gate: `bun run check` exits 0, `bun run check:bridge` passes, `bun test` passes, and `orch doctor` emits the same set of check ids recorded in task 0.2.

## 4. Phase D — `src/notify.ts` split

- [x] 4.1 Create `src/notify/` as three modules layered `format ← sinks ← router`: `format.ts` (pure formatter leaf — `notificationText`, `oneLine`, `payload`, label helpers), `sinks.ts` (builtin desktop/webhook/command providers, provider registration), `router.ts` (registry, `notify`, `deliverToSink`, `loadSinks`). Move verbatim; settings are JSON — no TOML parser. (2026-07-19: done, acyclic, no TOML parser.)
- [x] 4.2 Repoint importers of `src/notify.ts`; delete it. (2026-07-19: `src/notify.ts` and `src/notify-sinks.ts` both deleted, no stubs.)
- [ ] 4.3 Gate: `bun run check` exits 0, `bun run check:bridge` passes, and `bun test` passes.

## 5. Phase E — pi trust seeding relocation

- [x] 5.1 Move `TRUST_FILE`, `launchesPi`, `writeTrustEntry` from the (now former) command layer into `src/adapters/pi.ts`; repoint the spawn call site to the pi adapter. (2026-07-19: moved into `src/adapters/pi.ts`. spawn.ts cannot import a concrete adapter — `scripts/check-bridge.ts` bans that in core — so the call sites go through a new optional `AgentAdapter.preTrustWorkspace(cwd, cmd)` port method, gated on method presence like `diagnoseShim`. `grep -rn "trust.json\|writeTrustEntry\|launchesPi" src/commands/` returns nothing.)
- [ ] 5.2 Gate: `bun run check` exits 0, `bun run check:bridge` passes, `bun test` passes, and `grep -rn "trust.json\|writeTrustEntry\|launchesPi" src/commands/` returns nothing.

## 5b. Phase E2 — one shared presence writer (NOT a pure move — own gate)

- [x] 5b.1 Create `src/presence/writer.ts`: `atomicWrite`, presence-directory helpers, status/result writers. Create `src/presence/inbox.ts`: the claim-rename drain + `ack.jsonl` marker. Both node-safe and dependency-light per Rule 6 — the claude/codex shims run under node, deno, OR bun (`CLAUDE_HOOK_RUNTIMES`), so no `Bun.*` and no heavy transitive graph.
- [x] 5b.2 Define the four protocol filenames (`status.json`, `result.json`, `inbox.jsonl`, `answer.json`) as exported constants in `src/presence/`; replace every raw quoted occurrence elsewhere in `src/` and `extensions/` with the constant. (Constants defined in `src/presence/schema.ts` and adopted by the new writer + the claude/codex shims. The remaining core call sites are enumerated in `PRESENCE_SWEEP_PENDING` in `scripts/check-bridge.ts` — that list is the sweep checklist and must reach empty.)
- [x] 5b.3 All three harness artifacts import the shared writer rather than carrying their own copy: `extensions/pi/`, `extensions/claude/index.ts`, `extensions/codex/index.ts` each take `atomicWrite`, the presence directory and the status/result paths from `src/presence/writer.ts`, and pi's inbox drain and ack marker from `src/presence/inbox.ts`. `grep -rn 'function atomicWrite' src/ extensions/` reports exactly one definition, in `src/presence/writer.ts`.
- [x] 5b.4 Guard change (design D6 decision): remove the `{owner: "pi", literal: "inbox.jsonl"}` and `{owner: "pi", literal: "answer.json"}` entries from `ADAPTER_WIRE_LITERALS` in `scripts/check-bridge.ts` — orch defines those filenames, it does not conform to a third party's. Keep every codex and claude entry banned. Add the compensating rule: the raw quoted filenames are forbidden OUTSIDE `src/presence/`, so there is still exactly one definition site.
- [ ] 5b.5 **Gate (behavioral, not just green tests):** `bun run check` exits 0, `bun run check:bridge` passes, `bun test` passes — AND spawn a real agent under each installed harness, confirming `status.json` and `result.json` still land in `$ORCH_DIR/agents/<KEY>/` and `orch status` renders each. This phase changes what executes inside the claude/codex shims, which run outside the test harness; a green suite alone does NOT close it.

## 6. Phase F — module tests and purity audit

- [x] 6.1 Add or relocate direct unit tests so each new module is exercised in isolation (import the module, not the full CLI) — mirror the injectable-liveness style the backend/headless tests already use. (2026-07-17: 14 new `test/commands-*.test.ts` files, one per domain module — target parsing, status derivation, lifecycle fail-fast, result routing, queue round-trip, daemon validation, clean reaping, spawn/setup/events/review/control/panes/index helpers; targeted run 33 pass / 0 fail)
- [x] 6.2 Audit leaf command modules: confirm no leaf (outside `target.ts` and the resolving command entry) calls `loadConfig`/`resolveTarget`/`resolveBackend` or performs raw `fs` reads/writes against `~/.orch/agents`; narrow any that do. (2026-07-17: audited all 12 leaf modules; narrowed 5 config re-resolution leaks — `spawn.ts` threads the resolved `Backend` into `launchAdditionalAgents`/`reportSpawnResults` and loads config once in `cmdTile`; `control.ts` loads once per dispatch; `status.ts` loads once per status; `lifecycle.ts` hoists `loadConfig` above `cmdRestart`'s loop — and moved `clean.ts`'s raw presence-dir `rmSync` behind a new `store.ts` `removePresenceAgentDir` primitive. Borderline entry-level resolutions left as-is.)
- [x] 6.3 Audit function names across the moved modules for banned vague qualifiers (`Standard`/`Default`/`Resolved`/`Generic`/`Common`/`Handle`/`Process`/`Manage`/…); rename offenders and their callers (rename only — no logic change). (2026-07-17: only two banned-`Do` offenders — `doReload`→`reloadPaneAndAwaitBridge`, `doHardRestart`→`restartPaneAndAwaitBridge` in `lifecycle.ts`, callers + `test/commands-lifecycle.test.ts` updated; `resolveActiveDefault`/`switchDefault` are domain terms, not vague, left as-is.)

## 7. Phase G — verification (run the scenarios, do not self-certify)

- [x] 7.1 Run the ceiling check `find src extensions -name '*.ts' | xargs wc -l | sort -rn | head` and assert the top count is ≤ 700 and none of the four deleted monolith paths appear. (2026-07-19: top is `src/backends/herdr/hud.ts` at 644; all deleted monolith paths absent.)
- [x] 7.2 Execute every `module-layout` scenario against the tree: domain/pi/doctor/notify modules exist and are ≤ 700 lines; `extensions/` contains only per-harness directories and no loose `.ts`; the herdr grep over `extensions/` returns nothing and `src/backends/herdr/hud.ts` exists; exactly one `atomicWrite` definition; presence filenames appear only inside `src/presence/`; `src/commands/index.ts` has no business logic; deleted paths have zero re-exports (`grep`) and are untracked (`git ls-files`). (2026-07-19: every scenario passes. The sweep also caught `bin/pif` loading two source paths the Rule 10 move retired — repointed to `extensions/pi/index.ts`.)
- [ ] 7.3 Confirm phases A–E are a pure move: `git log --stat` for those phases shows relocations only, and no behavioral test assertion was modified. **Phase E2 (5b) is explicitly EXCLUDED from this check** — the presence dedup deliberately changes what executes in the claude/codex shims; it is covered by its own behavioral gate at 5b.5, not by the pure-move review.
- [ ] 7.4 Final gate: `bun run check` exits 0, `bun run check:bridge` passes, and full `bun test` passes on an idle machine.
