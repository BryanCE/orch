## 0. Sequencing gate (do not skip)

- [ ] 0.1 Confirm `monolith-file-breakdown` has completed **Phase C** (`src/doctor.ts` → `src/doctor/`) and **Phase E** (pi trust seeding into `src/adapters/pi.ts`): `openspec show monolith-file-breakdown` reports tasks 3.3 and 5.2 checked, and `src/doctor.ts` no longer exists. Per design D4 this is a hard prerequisite — every doctor edit below targets the post-split module paths. Do NOT start any group below until this holds.
- [x] 0.2 Record the current `orch doctor` check ids and the current `SETTINGS_SCHEMA` value, so group 7 can prove the check set grew by exactly one and the schema bumped by exactly one.
- [ ] 0.3 Back up the working `$ORCH_DIR/settings.json` — group 2 invalidates it by design, and having the pre-bump file makes the group 7 re-setup verifiable rather than guesswork.

## 1. Shared runtime vocabulary

- [x] 1.1 Create the shared runtime module exporting `ORCH_RUNTIMES = ["node","deno","bun"]`, the `OrchRuntime` type, `DEFAULT_RUNTIME`, and a `runtimeArgv` builder. Node-safe and dependency-light per Rule 6 — the claude and codex shims run under all three runtimes. (2026-07-19 revision: `runtimeCommand` returning a bare prefix string was replaced by `runtimeArgv`, for two reasons. (a) It emitted the BARE executable name, resolved through whatever PATH claude/codex had at spawn time — under nvm/fnm/volta/asdf or Windows-vs-WSL that can differ from orch's, so it now resolves an ABSOLUTE path via `binaryPath`, falling back to the bare name. (b) deno got `--allow-all`, which discards the sandbox that is the only substantive reason to choose deno; it now receives least-privilege grants derived from the shims' real surface — `--allow-read` scoped to $ORCH_DIR + shim + claude's transcript root, `--allow-write` scoped to $ORCH_DIR, an enumerated `--allow-env`, `--allow-sys=homedir`, and no `--allow-net`/`--allow-run`/`--allow-ffi`.)
- [x] 1.2 Delete `CLAUDE_HOOK_RUNTIMES` / `ClaudeHookRuntime` from the claude adapter's hook module and repoint `claudeHookCommand` at the shared mapping. Do NOT leave a re-export alias (design D2, Rule 8 — one current name).
- [x] 1.3 Repoint `test/claude-hooks-shim.test.ts`, which currently derives its runtime list from the deleted adapter constant.
- [x] 1.4 Correct the stale doc comment that asserts "orch never requires one specific runtime; node, deno, and bun all work" — that is the exact belief this change reverses.
- [ ] 1.5 Gate: ask the user to re-run `bun run check` and `bun test` and re-read `current-errors.md` / `test-fails.md`. Tree is shippable here — vocabulary moved, no behavior changed yet.

## 2. Settings schema (atomic — must land as one commit)

- [x] 2.1 Add required top-level `runtime` to the settings zod `strictObject` in `src/config.ts` and bump `SETTINGS_SCHEMA` from 1 to 2. Required, with **no** `?? "node"` default-on-read — an absent key is a loud error (design D5). Confirm it is top-level, not under `defaults` and not an `installed` set (design D1).
- [x] 2.2 In the SAME change, update every writer, reader, and fixture: the setup writer, `loadConfig`'s normalized `OrchConfig` shape, and every test constructing a settings object. Rule 8 forbids two live shapes — a half-migrated tree is the failure mode this task exists to prevent.
- [x] 2.3 Verify the existing `schemaVersion` mismatch path already names `orch setup` as the remedy for a schema-1 file; extend its message only if it does not.
- [ ] 2.4 Gate: ask the user to re-run `bun run check` and `bun test`. Expect every settings fixture to have been updated — a residual schema-1 fixture surfaces here, not in production.

## 3. Setup selects the runtime

- [x] 3.1 Add the runtime selection step to the setup wizard: a single-select of `node` / `deno` / `bun` with `node` pre-selected. All three are presented as supported (2026-07-19 revision: the prompt previously labelled bun "build tool only, UNSUPPORTED" and deno "UNVERIFIED" — a three-option menu where the copy told the user two options were broken. That misread Rule 6, which bans bun-dependent CODE (`Bun.*`, `bun:*`) so the tree runs anywhere; it does not bar a user from executing orch with bun. Labels now state the real differentiators: node most widely present, deno sandboxed shims, bun fastest startup.) orch must never probe PATH to pre-select — the recorded value is always a selection.
- [x] 3.2 Persist the selection as the top-level `runtime` key, idempotently on re-run — a repeat selection leaves the file unchanged, a different selection replaces in place and never accumulates.
- [x] 3.3 Delete the PATH auto-detection in the claude adapter (`CLAUDE_HOOK_RUNTIMES.find(binaryOnPath) ?? "node"`). Nothing may infer a runtime after this task.
- [ ] 3.4 Gate: ask the user to re-run `bun run check` and `bun test`, then run `orch setup` and confirm `settings.json` gains `"runtime": "node"` at top level with no `defaults.runtime` and no `installed.runtimes`.

## 4. Shims consume the declared runtime

- [x] 4.1 Repoint the Claude hooks installer at the declared runtime for all three hook events. (`src/adapters/claude.ts` `installShim` reads `declaredRuntime(orchDir())` and builds each event's command from it.)
- [x] 4.2 Repoint the codex notify shim installer at the declared runtime. DELETE `CODEX_NOTIFY_RUNTIMES` / `CodexNotifyRuntime` from `src/adapters/codex-notify.ts` — it is a second copy of the runtime vocabulary — and delete the PATH inference at `src/adapters/codex.ts:203` (`CODEX_NOTIFY_RUNTIMES.find(binaryOnPath) ?? "node"`). Leave no alias. After this task `grep -rnF '?? "node"' src/` returns nothing and `ORCH_RUNTIMES` is the sole runtime list. (2026-07-19: both constants deleted with no alias; `codexNotifyArgv` now delegates to `runtimeArgv`, which also removed codex's private `--allow-all` deno form; `installCodexNotifyShim` reads `declaredRuntime(orchDir())`. Verified: `grep -rnF '?? "node"' src/` returns nothing and no `CODEX_NOTIFY_RUNTIMES` reference survives.)
- [x] 4.3 NOT APPLICABLE — pi has no runtime to repoint. `PiAdapter.installShim` SYMLINKS the built bundle into `~/.pi/agent/extensions/`; the pi agent loads it in-process, so orch never writes a command string and never chooses a runtime for it. Only claude and codex are spawned by a host tool with an argv orch controls. (2026-07-19: recorded rather than left open — the proposal's "all three harness shims resolve the runtime" is true of the two that are spawned, not of pi.)
- [x] 4.4 Verify the spawned shims use the ONE shared invocation mapping from 1.1 — `grep` for a second deno-invocation string and confirm exactly one definition site exists. (2026-07-19: `ORCH_RUNTIMES` is the sole runtime list, `src/runtime.ts` the sole deno-permission site; `grep -rnF '?? "node"' src/` returns nothing.)
- [ ] 4.5 Gate: ask the user to re-run `bun run check` and `bun test`, then `orch setup` followed by inspection of `~/.claude/settings.json`, `~/.codex/config.toml`, and the pi extension install to confirm all three name the declared runtime.

## 5. Doctor verifies declared vs reality

- [x] 5.1 Add the `runtime` doctor check in the post-split doctor module. Primary signal is the executing runtime; the resolved-on-PATH `orch` entrypoint shebang is the secondary signal (design D3). (2026-07-19: added as `src/doctor/runtime.ts`, registered in `runner.ts` between `config` and `spawn-limits`. Deviation from design D3: the primary signal reads `process.versions` (`.bun`/`.deno`) rather than the basename of `process.execPath`. A basename is a guess — under a version manager or a renamed/symlinked binary the executable may not be called "node" at all — whereas the version table is the runtime naming itself and cannot disagree with what is executing.)
- [x] 5.2 Implement the verdict table in `src/doctor/runtime.ts`: declared==actual → ok; declared != the runtime actually executing orch → **fail**; resolved `orch` entrypoint shebang names a different runtime than declared → **fail** (fires even when doctor itself happens to run under the declared runtime, so a mismatched entrypoint cannot hide behind a lucky invocation); declared runtime absent from PATH → fail (shims spawned by claude/codex would fail silently at agent runtime); settings unreadable → skip, since `checkConfig` owns that detail. No runtime is privileged — `declared bun + actual bun` is a plain ok, NOT ok-plus-warn: the defect this check exists for is the mismatch, not which runtime won. Remediation names both directions (rebuild via `bun run build:dev`, or re-record via `orch setup --runtime <actual>`).
- [x] 5.3 Add unit tests for each verdict-table row, injecting the observed runtime rather than depending on the runtime the suite happens to run under. (2026-07-19: `test/doctor-runtime.test.ts`. `checkRuntime` takes an optional `RuntimeObservations` — running runtime, PATH resolver, entrypoint reader — so every row is deterministic: declared==actual is asserted for all three runtimes, and both directions of mismatch (node-declared/bun-running AND bun-declared/node-running) are asserted, since no runtime is privileged. Includes the motivating regression: entrypoint shebang mismatch fails even when the running runtime matches. `shebangRuntime` is covered separately against real files, including that `nodemon` does not read as `node`.)
- [ ] 5.4 Gate: ask the user to re-run `bun run check` and `bun test`, then `orch doctor` and confirm the `runtime` row reads ok on a correctly installed tree.

## 6. Doctor stops asserting what it did not verify

- [x] 6.1 Make the Claude hooks shim diagnostic confirm the shim file exists on disk before reporting current — registration in the host config is necessary but not sufficient. It currently prints the shim path as evidence of health for a file that may not exist (design D7). (2026-07-19: `src/adapters/claude.ts` `diagnoseShim` now returns `warn` with `run orch setup` when `claudeHookShimPath` is absent, before any hook comparison — matching what codex's `diagnoseShim` already did.)
- [x] 6.2 Audit the remaining doctor checks against the D7 rule: any check whose detail names a path must have confirmed that path. Fix every violation found, not only the Claude one. (2026-07-19: audited every `status: "ok"` detail across `src/doctor/*.ts` and both adapter `diagnoseShim`s. No violations remain — the surviving `ok` details name pids confirmed live via `pidAlive`, content hashes computed by actually reading the file, PATH resolutions confirmed via `accessSync`, or plain counts. No check reports `ok` while naming a path it did not stat.)
- [x] 6.3 Correct backend-capabilities gating (design D6): `available` is a conjunction over every installed backend; `insideSession` is required only of the active/default backend. The current rule is unsatisfiable with two pane backends installed. (2026-07-19: `src/doctor/backends.ts` fails on `unavailable` across all installed backends but tests `insideSession` only against the report `activeBackend` selects, which honours the configured default via a registry-map lookup and otherwise mirrors resolveBackend's probe order without throwing — capabilities only, no id equality branch.)
- [x] 6.4 Fix the backend-capabilities detail string, which joins entries with a literal `\n` and renders as one run-on line — one backend per line. (2026-07-19: rows join on a real newline + indent; the active backend is tagged `(active)`.)
- [x] 6.5 Add a regression test for the observed three-backend case (herdr active, headless + tmux installed, tmux `insideSession=false`) asserting it passes. (2026-07-19: `test/doctor-backends.test.ts` injects the reports rather than depending on whether the suite happens to run inside a herdr/tmux session, and covers the observed install plus multi-line rendering, active-outside-session, unavailable-backend, and configured-default-over-probe-order.)
- [ ] 6.6 Gate: ask the user to re-run `bun run check` and `bun test`, then `orch doctor` with herdr + headless + tmux installed and confirm `Backend capabilities` passes and renders multi-line.

## 7. Verification (run the scenarios, do not self-certify)

- [ ] 7.1 Execute every `runtime-selection` scenario against the tree, including the negative ones: a settings file with `runtime` omitted exits non-zero naming `orch setup`; `"runtime": "quickjs"` exits non-zero naming the three accepted values; `defaults.runtime` is rejected as misplaced.
- [ ] 7.2 Execute the `doctor-config`, `provider-setup`, `agent-adapters`, and `fleet-backends` delta scenarios.
- [ ] 7.3 Prove the mismatch check actually catches the original bug: point the `orch` entrypoint at a bun-shebang script while `runtime` is `node`, confirm `orch doctor` FAILS and names `bun run build:dev`, then restore. This is the regression that motivated the change — a green suite alone does not close it.
- [x] 7.4 Confirm the doctor check-id set grew by exactly one versus the 0.2 baseline, and `SETTINGS_SCHEMA` bumped by exactly one.
- [ ] 7.5 Final gate: ask the user to run `bun run check` and full `bun test`, then re-read `current-errors.md` and `test-fails.md`. Do not declare the change complete without user-provided passing output (Rule 5).
