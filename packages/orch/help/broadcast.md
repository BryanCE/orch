Steer the named targets through orchd and report which refused. `--all` is every agent the
caller holds an open lease on, and is operator-only: a spawned agent or a harness session is
refused. An unregistered shell holds nothing and is refused with the fix: spawn or adopt
first, or name the targets.
