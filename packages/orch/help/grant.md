Approve an action an agent was refused, such as opening a plexer home from outside a pane
or running a command in `gated_commands`.
Needs a terminal: no flag answers the prompt for you. `--list` shows what is waiting.
