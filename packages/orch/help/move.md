Move a pane to another tab, or to a fresh one with `--new-tab`. Never steals focus. Use it
only when a pane is already in the wrong tab; `spawn --tab <label>` puts a fleet where it
belongs from the start. Pass the tab id from `orch tabs`, never a label that two tabs share.
