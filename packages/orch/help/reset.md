Start a fresh session in the same pane, then pin `--model`, else the tuning the agent
holds, else that harness's `defaults.models` entry. A short model name expands like spawn's
`--model`.

`orch dispatch` already clears the session itself. Reset is for clearing the context
without sending work. reload = same session; reset = new session; restart = new process.

`--all` is every agent the caller holds an open lease on, and is operator-only.
