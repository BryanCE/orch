Durably accept a mid-run instruction through orchd. orchd pushes it down the agent's bridge
link, and the reply says whether the agent applied it.

Steer at most once per running agent. A doctrine change big enough to explain twice is a
new dispatch. An agent in `asking` refuses a steer and names `orch answer`.
