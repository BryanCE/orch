| claim | verdict PASS/ISSUE | file:line | smallest fix |
|---|---|---|---|
| 4.6 OS-side seam | **ISSUE** | `src/daemon/rpc.ts:482`; `src/process-identity.ts:18` | Replace the remaining raw `process.platform` branches with `osSide()` comparisons; the claimed one-seam rule is not complete. |
| 5.5 shared `isRecord` / `errorMessage` | **ISSUE** | `test/commands-setup.test.ts:20`; `test/work-notify.test.ts:16` | Import `isRecord` from `src/util.ts` in both tests and delete the two private copies. Runtime source callers are unified. |
| 2.1 Placement deletion/composed view | **ISSUE** | `test/no-placement-row-over-the-composed-view.test.ts:43-47` | The filesystem-absence test was already green at base `f7dc80e` (the file is absent there), so it cannot pin this landing; remove it or replace it with a regression test that fails when a projection is reintroduced. The moved-space behavior at lines 50-69 is meaningful. |
| 2.5 + 3.2 status row/table consolidation | **PASS** | `src/commands/status.ts:449-527`, `src/commands/status.ts:580-605` | — |
| 6.6 removal of unsafe test casts | **PASS** | `test/commands-setup.test.ts:56-91`; `test/commands-status.test.ts:20-153`; other changed fixtures | No forbidden cast was added in the diff; the changed fixtures are complete and the focused tests pass. |
| Rule 8, no compatibility paths | **ISSUE** | `src/doctor/presence.ts:59-63`; `src/types/doctor.ts:20-21` and `src/doctor/backends.ts:22,41,49,53`; `src/extensions/bundles.ts:13-14` and `src/adapters/pi.ts:327-332`; `src/types/notify.ts:55-56,68-69` | Remove the old-bridge “healthy” path, `available` fallback field, retired-bundle cleanup, and phase-1 notifier compatibility; keep only the current shape and error/reap malformed data. |
| Rule 13 | **PASS for this diff** | `git diff f7dc80e -- src test` | No `as unknown as`, `as never`, explicit `any`, or `@ts-expect-error` was introduced. Existing unrelated casts remain elsewhere in the baseline tree. |
| Rule 14 schema constants | **PASS** | `src/config.ts:26`; `src/presence/schema.ts:10` | Both remain `1`; no version moved. |
| Rule 6 runtime compatibility | **PASS** | `src/` and `extensions/` scan | No `Bun.*` call or `bun:*` import was introduced. |
| Weakened assertions | **PASS** | Changed tests in `test/` | No assertion was deleted or loosened; the notification title matcher was split into an equivalent explicit assertion. |
| Tautological tests | **ISSUE** | `test/no-placement-row-over-the-composed-view.test.ts:43-47` | See 2.1: this deletion assertion cannot fail against the stated base. The OS test is non-tautological because it asserts literal Windows/Linux values and persisted store output. |
| Dead code | **PASS** | `src/types/agent.ts` and `test/helpers/views.ts` | Removed `RegistryRetry` has no remaining references; new shared fixtures have importers. |
