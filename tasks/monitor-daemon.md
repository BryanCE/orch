# Daemon-owned fleet monitor

Implement the first daemon-owned monitor slice now. Bryan requires bare `orch monitor` to arm delivery through daemon outbox/bridge, not a Pi tool or JSON parser. Pi is a thin UI. This task is FLEET monitoring only; generic command execution follows after the fleet path works.

Own packages/orch/src/db/schema.ts, src/store/monitor-rows.ts (new), src/daemon/server/monitors.ts (new), src/daemon/server/handlers/monitor.ts (new), src/daemon/client/protocol.ts, src/daemon/server/handlers/table.ts, src/daemon/server/state.ts, src/daemon/server/events.ts, src/daemon/server/orchd.ts as needed, and new test/daemon-monitor.test.ts plus directly necessary RPC fixture coverage. Do NOT edit agent presence/tools/harness bridge or extensions/pi; another worker owns identity.

Contract: add RPC `monitor-start` with {caller?: CallerCredential}; idempotently starts this caller's fleet monitor. Add `monitor-list` with {caller?: CallerCredential}, `monitor-get` with {caller?: CallerCredential,id:string}, `monitor-stop` with {caller?: CallerCredential,id:string}. Derive owner from daemon-resolved caller, never trust owner/actor params. Refuse missing identity and missing attached recipient bridge on start, so shell calls can retain streaming CLI behavior. Start result is a MonitorView: {id:string,ownerId:string,source:{kind:'fleet'},startedAt:number,stoppedAt:number|null}; list result {monitors:MonitorView[]}; get result {monitor:MonitorView,output:{seq:number,at:number,text:string}[]}; stop result MonitorView. Reuse schemas/types as authoritative source, no duplicated interfaces.

Store normalized monitor records and output in store; no files, no extension-private state. Existing daemon event publication feeds active monitors using current monitor.on settings and agentInMineScope ownership, excluding self transitions. Format using notificationText. Queue each accepted notification as existing {action:'steer',text:...} outbox payload, do not invent another delivery transport. Monitor report delivery MUST not recursively monitor itself. Preserve existing worker mail routing; do not duplicate messages already delivered by bridge. Use existing retention settings for bounded output if semantically suitable; otherwise report exact required setting to me before hardcoding. Idempotent stop; list/get caller-scoped; events and output persisted together where needed. Reuse existing outbox drain/ACK. No changes to harness-specific code. No plexer identity branches.

IMPORTANT no drizzle directory edits, migration generation/runs, builds, publishing, daemon reloads or commits. If new table test execution requires an absent migration, report blocker and do not run that test; code/schema edits and bun check are allowed. Do not add runtime DDL workaround. Run safe direct tests once only after final edits when runnable. Gate errors in other workers' files mean pending, report exact files. Return complete final report with changed files, check output, tests or migration blocker, exact follow-up needed.
