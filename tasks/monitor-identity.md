# Fix bridge identity before monitor delivery

Implement now. Own only src/agent/presence.ts, src/agent/tools.ts, src/agent/harness-bridge.ts, src/agent/daemon-client.ts, src/types/agent.ts, and directly relevant identity/presence tests under packages/orch/test. Paths are under packages/orch except this task file. Coordinate any necessary additional file before editing. Other workers own CLI monitor and daemon monitor delivery; do not touch those.

Remove interactive presence's independently minted identity. Use authenticated daemon register-session, with actual harness session token from ctx.sessionManager.getSessionId() and process.pid, before attach/status publication. Use existing registration claim builders where safe; no plexer/provider lookup in harness code and no direct store access. The daemon is sole identity issuer. Spawned workers retain launch credential. Lifecycle startup must await registration; late registration after shutdown must not attach/report. Missing daemon must not fabricate identity. Use daemon reconnect/startup retry machinery where possible. Reload with same session token resolves same daemon ID; changing session token must not retain previous identity. Keep peer tools reading the same presence identity as monitor/CLI.

No builds, migrations, installed writes, reloads, commits, real terminal tests or test:git. Read CLAUDE.md, architecture and skills. Run fallow before new helpers. Run bun check and safe direct test files once only after edits complete. Report actual changed files, clean gate output, tests and remaining issues. If gate errors are exclusively another worker's files report pending with filenames. Final result must contain the full report; do not send it only to spawner because routing is currently broken.
